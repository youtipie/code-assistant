{
  "knowledge": {
    "url": "http://knowledge:8080/mcp",
    "description": "Our indexed snapshot of the Saleor code and docs, pinned at a commit."
  },
  "github": {
    "url": "https://api.githubcopilot.com/mcp/",
    "description": "Live github.com. Use for history, commits, pull requests and issues -- the indexed snapshot cannot answer those.",
    "requires_env": "GITHUB_TOKEN",
    "headers": {
      "Authorization": "Bearer ${GITHUB_TOKEN}",
      "X-MCP-Readonly": "true",
      "X-MCP-Tools": "list_commits,get_commit,search_pull_requests,pull_request_read,issue_read,search_issues,get_file_contents"
    }
  }
}
