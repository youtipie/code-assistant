[project]
name = "ingestion"
version = "0.1.0"
description = "Corpus ingestion: clone, walk, classify, load"
requires-python = ">=3.12"
dependencies = [
    "core",
    "pyyaml>=6.0",
    "typer>=0.15.1",
    "ranx>=0.3.20",
    "rich>=13.0",
]

[tool.uv]
package = false

[tool.uv.sources]
core = { workspace = true }
